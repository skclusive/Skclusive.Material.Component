@namespace Skclusive.Material.Form
@inherits StyleComponentBase

<Styled
    Name="FormHelperText">

    .FormHelperText-Root {
        color: var(--theme-palette-text-secondary, rgba(0, 0, 0, 0.54));
        margin: 0;
        margin-top: 8px;
        min-height: 1em;
        text-align: left;
        line-height: 1em;

        font-size: var(--theme-typography-caption-font-size, 0.75rem);
        font-family: var(--theme-typography-caption-font-family, "Roboto", "Helvetica", "Arial",
            sans-serif);
        font-weight: var(--theme-typography-caption-font-weight, 400);
        letter-spacing: var(--theme-typography-caption-letter-spacing, 0.03333em);
    }

    .FormHelperText-Root.FormHelperText-Disabled {
        color: var(--theme-palette-text-disabled, rgba(0, 0, 0, 0.38));
    }

    .FormHelperText-Root.FormHelperText-Error {
        color: var(--theme-palette-error-main, #f44336);
    }

    .FormHelperText-Margin-Dense {
        margin-top: 4px;
    }

    .FormHelperText-Contained {
        margin: 8px 14px 0;
    }

</Styled>