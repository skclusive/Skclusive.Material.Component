@namespace Skclusive.Material.Input
@inherits InputComponent

<InputBase
    RootRef="@RootRef"
    @ref="Input"
    Class="@_Class"
    Style="@_Style"
    @attributes="Attributes"
    Error="@_Error"
    Focused="@_Focused"
    Disabled="@_Disabled"
    AutoComplete="@AutoComplete"
    AutoFocus="@AutoFocus"
    DefaultValue="@DefaultValue"
    FullWidth="@FullWidth"
    InputComponent="@Component"
    Multiline="@Multiline"
    Name="@Name"
    PlaceHolder="@PlaceHolder"
    ReadOnly="@ReadOnly"
    Rows="@Rows"
    RowsMax="@RowsMax"
    Select="@Select"
    Type="@Type"
    Value="@Value"
    OnChange="@OnChange"
    OnFocus="@HandleFocus"
    OnBlur="@HandleBlur"
    HasStartAdornment="@__HasStartAdornment"
    HasEndAdornment="@HasEndAdornment"
    InputClass="@InputClass">
    <StartAdornment>
        @StartAdornment
    </StartAdornment>
    <EndAdornment>
        @EndAdornment
    </EndAdornment>
</InputBase>
